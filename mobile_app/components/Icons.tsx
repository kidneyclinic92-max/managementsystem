// Simple icon components for React Native
// Replace with react-native-vector-icons or similar in production

import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export const Package = ({ size = 24, color = '#000' }: { size?: number; color?: string }) => (
  <Text style={{ fontSize: size, color }}>📦</Text>
);

export const ShoppingCart = ({ size = 24, color = '#000' }: { size?: number; color?: string }) => (
  <Text style={{ fontSize: size, color }}>🛒</Text>
);

export const History = ({ size = 24, color = '#000' }: { size?: number; color?: string }) => (
  <Text style={{ fontSize: size, color }}>📋</Text>
);

export const User = ({ size = 24, color = '#000' }: { size?: number; color?: string }) => (
  <Text style={{ fontSize: size, color }}>👤</Text>
);

export const Search = ({ size = 24, color = '#000' }: { size?: number; color?: string }) => (
  <Text style={{ fontSize: size, color }}>🔍</Text>
);

export const Scan = ({ size = 24, color = '#000' }: { size?: number; color?: string }) => (
  <Text style={{ fontSize: size, color }}>📷</Text>
);

export const Trash2 = ({ size = 24, color = '#000' }: { size?: number; color?: string }) => (
  <Text style={{ fontSize: size, color }}>🗑️</Text>
);

export const Plus = ({ size = 24, color = '#000' }: { size?: number; color?: string }) => (
  <Text style={{ fontSize: size, color }}>➕</Text>
);

export const Minus = ({ size = 24, color = '#000' }: { size?: number; color?: string }) => (
  <Text style={{ fontSize: size, color }}>➖</Text>
);

export const Send = ({ size = 24, color = '#000' }: { size?: number; color?: string }) => (
  <Text style={{ fontSize: size, color }}>📤</Text>
);

export const CheckCircle = ({ size = 24, color = '#000' }: { size?: number; color?: string }) => (
  <Text style={{ fontSize: size, color }}>✅</Text>
);

export const XCircle = ({ size = 24, color = '#000' }: { size?: number; color?: string }) => (
  <Text style={{ fontSize: size, color }}>❌</Text>
);

export const Clock = ({ size = 24, color = '#000' }: { size?: number; color?: string }) => (
  <Text style={{ fontSize: size, color }}>⏰</Text>
);

export const LogOut = ({ size = 24, color = '#000' }: { size?: number; color?: string }) => (
  <Text style={{ fontSize: size, color }}>🚪</Text>
);

export const X = ({ size = 24, color = '#000' }: { size?: number; color?: string }) => (
  <Text style={{ fontSize: size, color }}>✕</Text>
);

















